node_modules/
.next/
*.tsbuildinfo
.env
.env.local
.env.*.local
/reports/
test-results/
# Environment-specific revenue runtime data; commit test-data/deployments.sample.json instead.
test-data/deployments.json
test-data/analytics.csv
playwright/.cache/
dist/
.auth/
# Vercel CLI output — generated locally by `vercel pull` / `vercel build`.
# Project linking (.vercel/project.json) is per-developer and should never
# be committed; CI re-creates it from VERCEL_ORG_ID / VERCEL_PROJECT_ID.
.vercel/
# Generated by scripts/prepare-dashboard-assets.ts during Next/Vercel builds.
/public/artifacts/
/public/dashboard-snapshot.json
.vercel
